import * as ping from "./ping";
import * as create_theme from "./create_theme";
import * as create_plugin from "./create_plugin";

export const commands = {
  ping,
  create_plugin,
  create_theme,
};
