import * as ping from "./ping";
//import * as create_theme from "./createTheme";
import * as createPlugin from "./createPlugin";

export const commands = {
  ping,
  createPlugin,
  //create_theme,
};
